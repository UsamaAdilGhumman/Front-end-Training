import { Link } from "react-router-dom";

const Button = (props) => {
    return(
        <>
            <button onClick={props.onClick} type="submit" >
                
                </button>
        </>
    )
}

export default Button;